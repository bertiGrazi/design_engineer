import { Component } from "react";

export default class Contador extends Component {
  render() {
    return (
      <div>
        <h1 style={
          {color: "black"}
        }>Contador (usando classe)</h1>
      </div>
    )
  }
}